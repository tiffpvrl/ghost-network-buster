# Optional: when backend DEMO_API_KEY is set, paste the same value here.
# VITE_DEMO_API_KEY=

# Production: your Cloud Run / API origin (omit in local dev; Vite proxies /api)
# VITE_API_BASE=https://your-api.run.app
